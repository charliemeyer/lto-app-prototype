"use client";

import { Header } from "@/components/Header";
import { SearchTabs } from "@/components/SearchTabs";

const Home = () => {
    return (
        <>
            <Header />
            <SearchTabs />
        </>
    );
};

export default Home;
